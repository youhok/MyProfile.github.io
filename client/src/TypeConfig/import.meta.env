interface ImportMetaEnv {
    BASE_URL: string;
    // Add other properties as needed
}

// Tell TypeScript about the structure of import.meta
declare global {
    var import: {
        meta: {
            env: ImportMetaEnv;
        };
    };
}